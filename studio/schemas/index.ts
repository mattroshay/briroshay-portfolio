import artwork from './artwork'
import localeString from './localeString'
import localeText from './localeText'
import localeSlug from './localeSlug'

export const schemaTypes = [artwork, localeString, localeText, localeSlug]
